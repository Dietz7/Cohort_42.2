let text = "Hello, world!"

// length of the string

let length = text.length; // 13
console.log(length);

// to UpperCase and to LowerCase

let upperCase = text.toUpperCase();// "HELLO, WORLD!"
let lowerCase = text.toLowerCase; // "hello, world!"

// getting of the substring

let substring = text.substring(0, 5);

// search of the substring


let indexOfWorld = text.indexOf("World") // 7
console.log(indexOfWorld);
